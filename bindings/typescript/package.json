{
  "name": " @bubbleproc/node",
  "version": "1.0.0",
  "description": "Bubblewrap sandboxing for Node.js - protect against AI coding tool damage",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "native"
  ],
  "scripts": {
    "build": "npm run build:native && npm run build:ts",
    "build:native": "cd native && cargo build --release && cp target/release/libbubbleproc_node.so ../src/native/index.node",
    "build:ts": "tsc",
    "test": "jest",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "sandbox",
    "bubblewrap",
    "security",
    "subprocess",
    "isolation",
    "napi"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    " @types/jest": "^29.5.0",
    " @types/node": "^20.0.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "napi-rs": "^2.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "os": [
    "linux"
  ]
}